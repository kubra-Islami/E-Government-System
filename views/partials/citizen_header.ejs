<nav class=" navbar navbar-expand-lg navbar-light bg-white shadow-sm sticky-top">
    <div class="container">
        <!-- Brand -->
        <a class="navbar-brand fw-bold text-primary d-flex align-items-center" href="/citizen/dashboard">
            <i class="bi bi-house-door-fill me-2"></i>
            <span>E-Gov Portal</span>
        </a>

        <!-- Mobile toggle -->
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#citizenNavbar" aria-controls="citizenNavbar" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <!-- Nav content -->
        <div class="collapse navbar-collapse justify-content-end" id="citizenNavbar">
            <ul class="navbar-nav align-items-center gap-3">
                <!-- Notifications -->
                <li class="nav-item">
                    <a class="nav-link position-relative" href="/citizen/notifications" aria-label="Notifications">
                        <i class="bi bi-bell-fill fs-5"></i>
                        <%
                        const unreadCount = notifications ? notifications.filter(n => !n.read).length : 0;
                        if (unreadCount > 0) {
                        %>
                            <span class="position-absolute translate-middle badge rounded-pill bg-danger shadow-sm" style="top: 25%">
                                 <%= unreadCount %>
                            </span>
                        <% } %>
                    </a>

                </li>

                <!-- User dropdown -->
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle d-flex align-items-center" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                        <i class="bi bi-person-circle fs-5 me-2 text-secondary"></i>
                        <span class="fw-semibold"><%= user.name %></span>
                    </a>
                    <ul class="dropdown-menu dropdown-menu-end shadow-sm rounded-3">
                        <li>
                            <a class="dropdown-item d-flex align-items-center" href="/citizen/profile">
                                <i class="bi bi-person me-2 text-primary"></i> Profile
                            </a>
                        </li>
                        <li><hr class="dropdown-divider"></li>
                        <li>
                            <a class="dropdown-item text-danger d-flex align-items-center" href="/logout">
                                <i class="bi bi-box-arrow-right me-2"></i> Logout
                            </a>
                        </li>
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>

<style>
    .navbar-brand span {
        font-size: 1.1rem;
    }
    .nav-link {
        color: #555 !important;
        transition: color 0.2s ease-in-out;
    }
    .nav-link:hover {
        color: #0d6efd !important;
    }
    .badge {
        font-size: 0.7rem;
        padding: 0.4em 0.6em;
    }
</style>
