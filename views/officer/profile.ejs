<div class="container py-4">
    <%- include('../breadcrumb', {
    breadcrumbs: [
    { label: "Dashboard", url: "/officer/dashboard" },
    { label: "Profile" }
    ]
    }) %>
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card shadow-sm border-0">
                <div class="card-body">
                    <h3 class="fw-bold text-primary mb-4">My Profile</h3>

                    <form action="/officer/profile" method="POST">
                        <div class="mb-3">
                            <label class="form-label fw-semibold">Name</label>
                            <input type="text" class="form-control" name="name" value="<%= officer.name %>" required>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold">Email</label>
                            <input type="email" class="form-control" name="email" value="<%= officer.email %>" required>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold">Department</label>
                            <input type="text" class="form-control" value="<%= officer.department_name %>" disabled>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-semibold">Job Title</label>
                            <input type="text" class="form-control" name="job_title" value="<%= officer.job_title %>">
                        </div>

                        <div class="d-flex justify-content-end">
                            <button type="submit" class="btn btn-primary fw-semibold">Update Profile</button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>